{
  "name": "lacordedesol",
  "version": "1.0.0",
  "description": "Accordeur contrebasse — La Corde de Sol",
  "scripts": {
    "build": "mkdir -p www && cp index.html styles.css audio.js tuner.js positions.js app.js www/",
    "sync": "npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
